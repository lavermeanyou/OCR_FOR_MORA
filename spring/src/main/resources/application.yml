server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/mora
    username: mora
    password: mora1234
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect

app:
  jwt-secret: morasecret123
  jwt-expiration: 86400000
  ocr-service-url: http://localhost:8000
  openai-api-key: ${OPENAI_API_KEY:}
